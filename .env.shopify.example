# ================================================================
# SHOPIFY OAUTH CONFIGURATION
# ================================================================
# Required for Shopify integration to work properly
# ================================================================

# Shopify App Credentials (Get from Shopify Partner Dashboard)
SHOPIFY_API_KEY=your_shopify_api_key_here
SHOPIFY_API_SECRET=your_shopify_api_secret_here

# OAuth Redirect URI (Must match the one configured in Shopify Partner Dashboard)
# For development: http://localhost:3001/api/shopify-oauth/callback
# For production: https://api.ordefy.io/api/shopify-oauth/callback
SHOPIFY_REDIRECT_URI=http://localhost:3001/api/shopify-oauth/callback

# Shopify API Version (Use the latest stable version)
SHOPIFY_API_VERSION=2024-10

# Shopify Scopes (Permissions your app needs)
# Default: read_products,write_products,read_orders,write_orders,read_customers,write_customers
SHOPIFY_SCOPES=read_products,write_products,read_orders,write_orders,read_customers,write_customers

# Application URLs
# Frontend URL (where users will be redirected after OAuth)
APP_URL=http://localhost:8080

# Backend API URL (where webhooks will be sent)
API_URL=http://localhost:3001

# ================================================================
# TESTING
# ================================================================
# To test your configuration, run:
# curl http://localhost:3001/api/shopify-oauth/health
#
# You should see:
# {
#   "configured": true,
#   "missing_vars": [],
#   "config": { ... },
#   "message": "Shopify OAuth is properly configured"
# }
# ================================================================
